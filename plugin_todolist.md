- deck validator feature
- pre-action hooks allow UI to display warning. UI has a disable warnings feature. "are you sure? Evolving normally illegal."
- UI needs a 'search for card' feature
- need an 'in progress' zone. this staging zone will also be useful for other effects. when you select card(s) from your deck via search it just moves to this 'in progress'. staging zone is controlled by the game and is present regardless of playmat
should probably be on bottom of screen. highlight the text and outline bright red if a card is there, to remind people to use it. zone is a visible, unordered stack of cards.
- need a 'arrange top N cards' or 'arrange bottom N cards' modal
- need a way to shuffle a zone
- need 'peek top N' feature

